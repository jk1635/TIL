### Github 페이지 만들기(jekyll)

```shell
$ cd C:/Users/JK/Documents/XXX

$ git clone https://github.com/XXX/XXX.github.io.git

$ cd ./XXX.github.io

# 해당 폴더에 테마 넣기

$ gem install bundler

$ bundle install

$ git add .

$ git commit -m "Theme"

$ git push

$ git push -f
# 강제로 push
